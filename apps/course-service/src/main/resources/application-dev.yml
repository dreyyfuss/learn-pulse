spring:
  datasource:
    url: jdbc:mysql://mysql:3306/course_service_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: learnpulse
    password: learnpulse

  jpa:
    show-sql: true

  data:
    redis:
      host: redis
      port: 6379

  kafka:
    bootstrap-servers: kafka:9092

app:
  s3:
    endpoint: http://minio:9000

logging:
  level:
    com.courseservice: DEBUG
    org.springframework.security: DEBUG
