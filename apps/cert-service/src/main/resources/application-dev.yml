spring:
  datasource:
    url: jdbc:mysql://mysql:3306/learnpulse_certs?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: learnpulse
    password: learnpulse

  jpa:
    show-sql: true

  kafka:
    bootstrap-servers: kafka:9092

app:
  s3:
    endpoint: http://minio:9000

logging:
  level:
    com.certservice: DEBUG
    org.springframework.security: DEBUG
